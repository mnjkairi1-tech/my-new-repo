// import admin from 'firebase-admin';

// // Check if the app is already initialized to prevent errors
// if (!admin.apps.length) {
//   // When deployed to Firebase App Hosting or other Google Cloud environments,
//   // the SDK automatically discovers the service account credentials.
//   // For local development, you would need to set the
//   // GOOGLE_APPLICATION_CREDENTIALS environment variable.
//   admin.initializeApp();
// }

// const auth = admin.auth();
// const firestore = admin.firestore();

// export { admin, auth, firestore };
